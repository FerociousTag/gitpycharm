#zachary blackwell 1941472

#defines the function
def selection_sort_descend_trace(user_numbers):

if __name__=="__main__":
    user_numbers = []
    #inputs the users number into a string
    user_numbers_string = input().split(" ")
    #take the string of numbers and turns it into integers
    user_numbers = list(map(int, user_numbers_string))

    print(user_numbers_string)
    print(user_numbers)
    # selection_sort_descend_trace(user_numbers)
